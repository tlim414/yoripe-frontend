import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#4CAF50",
    },
    secondary: {
      main: "#FF7043",
    },
    background: {
      default: "#F8F9FA",
    },
  },
  shape: {
    borderRadius: 12,
  },
});